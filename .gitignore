# Ignored files/directories that are typically generated or environment-specific
/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor

# Environment specific files
.env                 # This ignores your actual .env file, crucial for security
.env.backup
.env.production      # Also ignores specific production environment files if they exist
.env.example

# Other development/IDE specific files
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed

# Keep .env.example tracked for project setup instructions
# This line is NOT in the .gitignore, allowing .env.example to be committed